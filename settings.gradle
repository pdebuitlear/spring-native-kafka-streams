pluginManagement {
    repositories {
        maven { url binFrogPluginRepoUrl }
        maven { url pluginRepoUrl }
        maven { url springReleases}
        maven { url springSnapshots}
    }
}

rootProject.name = 'spring-native-kafka-streams'

